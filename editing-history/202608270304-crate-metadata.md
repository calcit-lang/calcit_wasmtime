# Complete crates.io package metadata

- Added the required crate description and SPDX license identifier after crates.io rejected the 0.1.2 upload.
- Added repository and README metadata so the published package links back to its source and usage guide.
- Added `cargo package` to release verification before retrying the existing unpublished version.
